package com.commandos.commands;

/**
 * Marker interface per comandi documentati (opzionale).
 */
public interface Documented {
    /**
     * Restituisce la documentazione breve del comando.
     */
    String doc();
}
